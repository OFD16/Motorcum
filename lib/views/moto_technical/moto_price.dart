
import 'package:flutter/material.dart';

class MotoPrice extends StatelessWidget {
  const MotoPrice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('MotoPrice'),
    );
  }
}
